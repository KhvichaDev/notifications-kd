name: Publish to npm

on:
  release:
    types: [published]

jobs:
  publish:
    uses: KhvichaDev/npm-trusted-publisher/.github/workflows/kd-npm-publish.yml@main
    permissions:
      contents: write
      id-token: write
